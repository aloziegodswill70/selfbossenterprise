/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./app/**/*.{js,jsx}",
  ],
  theme: {
    extend: {
      colors: {
        gold: "#FFD700",  // Adjust if needed to match logo
        black: "#000000",
      },
    },
  },
  plugins: [],
}
